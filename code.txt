if(Announcement_For == "student form")
{
	temp = Add_Grades[Grades == input.Grades.Grades].student.getAll();
	temp2 = Add_student_form[ID in temp];
	for each rec in temp2
    {
		sendmail
        [
        	from: zoho.adminuserid
        	to: rec.Email1
        	subject: "Mail from college"
        	message: "Dear parents,<div><br></div><div>" + input.Information + "</div>"
			content type :HTML
        ]
    } 
}
else if ( Announcement_For== "Faculty form") 
{
	Fac = Add_Faculty_form[Departments == input.Departments];
	info Fac.Email.getall();
	info input.Departments;
	for each rec4 in Fac
	{
		info rec4.Email;
		sendmail
        [
        	from: zoho.adminuserid
        	to: rec4.Email
        	subject: "Mail from college"
        	message: "Greatings!<div><br></div><div>" + input.Information + "<br></div><div><br></div><div><br></div>"
			content type :HTML
        ]
		}
	}
	
	
